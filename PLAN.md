# 구현 계획: 공유 및 피드백 시스템

사용자님의 요청에 따라 **공유/저장 기능**과 **피드백 시스템**을 아래와 같이 구현하겠습니다.

## 1. 이미지 저장 및 공유 (`/share`)
**목표**: 만다라트 그리드를 고해상도 이미지로 저장하고 다른 앱으로 공유합니다.
- **주요 기능**:
  - **이미지 미리보기**: 현재 작성한 만다라트를 캡처용 뷰로 렌더링
  - **옵션 설정**:
    - 제목/날짜 표시 (ON/OFF)
    - 배경 모드 (Light/Dark/Transparent)
  - **액션**: 
    - `PNG 다운로드` (html2canvas 사용)
    - `공유하기` (Web Share API 사용)
- **참고**: 요청하신 대로 하단의 "투두앱 프로모션" 섹션은 **제외**하고 구현합니다.

## 2. 피드백 및 설문 (`/feedback`)
**목표**: 사용자로부터 서비스 만족도와 개선 의견을 수집합니다.
- **주요 기능**:
  - **만족도 조사**: 1~5점 척도 선택
  - **상세 의견**: 자유 텍스트 입력 (좋았던 점, 필요한 기능 등)
  - **대기자 등록**: 이메일/카카오ID 수집 (얼리 액세스용)
- **데이터 저장**:
  - Supabase에 `feedbacks` 테이블을 생성하여 데이터를 안전하게 저장합니다.

## 3. 진행 순서
1. `html2canvas` 라이브러리 설치 (완료)
2. Supabase `feedbacks` 테이블 생성 SQL 작성
3. `/share` 페이지 및 캡처 로직 구현
4. `/feedback` 페이지 및 데이터 전송 로직 구현

이 계획대로 즉시 구현을 시작하여 완성된 모습을 보여드리겠습니다.
